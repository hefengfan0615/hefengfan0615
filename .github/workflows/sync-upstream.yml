name: "Sync upstream"

on:
  schedule:
    # Weekly: Monday 03:00 UTC (Monday 11:00 Beijing time)
    - cron: '0 3 * * 1'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: sync-upstream
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout target repo
        uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0

      # Switch to a fresh local 'main' derived from origin/main, while the
      # working tree is still clean, so the sync changes are committed on main.
      - name: Ensure main branch
        run: |
          set -euxo pipefail
          git fetch origin
          if git show-ref --verify --quiet refs/remotes/origin/main; then
            git checkout -B main origin/main
          else
            git checkout -B main
          fi

      - name: Clone source repo (master branch)
        run: |
          set -euxo pipefail
          rm -rf /tmp/source
          git clone --depth 1 --branch master \
            https://github.com/hefengfan0615/hefengfan0615.github.io.git \
            /tmp/source

      - name: Save current workflows (must be preserved)
        run: |
          set -euxo pipefail
          rm -rf /tmp/workflows
          mkdir -p /tmp/workflows
          cp -a .github/workflows/. /tmp/workflows/

      - name: Replace every file with source, except this workflow
        run: |
          set -euxo pipefail
          shopt -s dotglob
          cd "$GITHUB_WORKSPACE"
          # Remove every file/folder except .git (this also removes root .github)
          find . -mindepth 1 -maxdepth 1 ! -name '.git' -exec rm -rf {} +
          # Copy all source files (including dotfiles) into the repo root
          cp -a /tmp/source/. ./
          # Restore our own workflows so this workflow is never disabled/removed
          mkdir -p .github/workflows
          cp -a /tmp/workflows/. .github/workflows/

      - name: Commit and push if changed
        run: |
          set -euxo pipefail
          cd "$GITHUB_WORKSPACE"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Sync files with hefengfan0615/hefengfan0615.github.io [skip ci]"
            # Authenticate the push with the built-in GITHUB_TOKEN.
            git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
            git push origin main
          fi